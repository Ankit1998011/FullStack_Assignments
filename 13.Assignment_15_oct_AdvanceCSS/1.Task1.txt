Ques 1. Explain the purpose of using the var() function in css.

Ans.

The var() function in CSS is used to insert the value of a custom property (also known as a CSS variable) into a style rule. 
CSS variables are custom values that can be reused throughout a document, making it easier to maintain and update styles.